import React, { useState } from 'react';

const Study = () => {
  const [num, setNum] = useState<Number>(0);
  const ll = 20;
  console.log(1243);
  const mml = 32;
  return (
    <div>
      123{ll}-{num}
    </div>
  );
};

export default Study;
